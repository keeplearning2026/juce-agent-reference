# .agent directory

Contains machine-readable progress and blocker state for unattended goal execution.

- `progress.json` — Current execution phase, completed phases, next action.
- `blocker.json` — Only exists when an unrecoverable external condition blocks execution.
